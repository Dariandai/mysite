---
import BaseLayout from '../../layouts/BaseLayout.astro';

const projects = [
  {
    slug: 'project-alpha',
    titleEn: 'File Tools',
    titleCn: '本地文件智能检索与问答',
    category: 'Python / RAG',
    image: 'https://img.dar1an.dpdns.org/Picgo/2026/03-09-02-46-33.png'
  },
  {
    slug: 'system-ui',
    titleEn: 'System UI',
    titleCn: '后台数据可视化界面',
    category: 'Dashboard',
    image: 'https://images.unsplash.com/photo-1550745165-9bc0b252726f?q=80&w=2670&auto=format&fit=crop'
  }
];
---

<BaseLayout title="Portfolio | Dar1an" description="精选项目作品集">
  <div class="page-content">
    <header class="page-header">
      <h1 class="page-title">
        <span class="en">Portfolio</span>
        <span class="cn">作品集</span>
      </h1>
    </header>

    <div class="projects-grid">
      {projects.map(project => (
        <a href={`/portfolio/${project.slug}/`} class="project-card">
          <div class="project-image">
            <img src={project.image} alt={project.titleEn} />
          </div>
          <div class="project-info">
            <span class="project-category">{project.category}</span>
            <h2 class="project-title">
              <span class="en">{project.titleEn}</span>
              <span class="cn">{project.titleCn}</span>
            </h2>
          </div>
        </a>
      ))}
    </div>

  </div>
</BaseLayout>

<style>
  .page-content {
    padding: 100px 10% 50px 140px;
    min-height: 100vh;
    width: 100%;
    box-sizing: border-box;
  }

  .page-header {
    margin-bottom: 60px;
  }

  .page-title {
    font-size: 3rem;
    font-weight: 300;
    margin-bottom: 15px;
  }

  .page-title .cn {
    font-size: 1.2rem;
    color: var(--text-muted);
    margin-top: 10px;
    display: block;
  }

  .page-desc {
    color: var(--text-muted);
    font-size: 1.1rem;
  }

  .projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    gap: 30px;
  }

  .project-card {
    background: var(--card-bg);
    border-radius: 12px;
    overflow: hidden;
    border: 1px solid rgba(255,255,255,0.05);
    transition: all 0.3s ease;
  }

  .project-card:hover {
    transform: translateY(-5px);
    border-color: rgba(255,255,255,0.2);
  }

  .project-image {
    height: 250px;
    overflow: hidden;
  }

  .project-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }

  .project-card:hover .project-image img {
    transform: scale(1.05);
  }

  .project-info {
    padding: 25px;
  }

  .project-category {
    color: var(--text-muted);
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 2px;
  }

  .project-title {
    margin-top: 10px;
  }

  .project-title .en {
    font-size: 1.3rem;
    font-weight: 500;
  }

  .project-title .cn {
    font-size: 0.9rem;
    margin-top: 5px;
  }

  @media (max-width: 768px) {
    .page-content {
      padding: 100px 5% 80px;
    }

    .page-title {
      font-size: 2rem;
    }

    .projects-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
